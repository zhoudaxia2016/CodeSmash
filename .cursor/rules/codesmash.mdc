---
description: CodeSmash Web 结构与组件约定（pages、components、hooks、utils）
globs: ["web/**/*"]
alwaysApply: true
---

## Web 前端：`pages` 与组件约定（`web/src`）

### 页面目录

- 与现有结构一致：功能放在目录里，入口为 **`index.tsx`**。  
  例：`pages/battle/compare/index.tsx`。
- 避免在 `pages/<area>/` 根下堆单文件大组件（如单独的 `*-drawer.tsx`）；新功能优先 **`pages/<area>/<feature>/index.tsx`**。

### 组件命名

- 导出组件用**领域/功能语义**（如 `NewProblem`、`Compare`），**不要用纯 UI 形态**作主名称（如 `NewProblemDrawer`、`FooModal`），除非该文件就是通用底层 primitive。

### 页面模块内的 Props 类型

- 若该 `index.tsx` **只有一份**组件入参类型且无同名冲突，使用模块内 **`type Props = { ... }`**（**不**使用 `NewProblemProps` 这类重复前缀命名），**不必导出**，除非其它文件需要扩展该类型。

### `components/` 与逻辑复用

- **功能目录名**用领域/动作（如 `create-problem`），**不要**绑定具体实现形态（如 `create-problem-form`）。
- **入口必须是 `index.tsx`**：主组件定义写在 `components/<feature>/index.tsx`，**不要**用 `index.ts` 只做 re-export 桶。
- 同目录下可并列子模块（如 `problem-authoring-assist-panel.tsx`）。**不要**从 `components` 再导出 `utils` 等纯函数；纯函数放在 **`web/src/utils/`**，按领域命名（如 `test-case-rows.ts`），避免以单一动词堆叠的长文件名（如 `parse-test-case-rows.ts`）。

### 大模型 / 接口编排

- 调用命题辅助、mutation 等与 **BYOK API** 相关的编排放在 **`hooks/`**（如 `useProblemAuthoringAssist`）；表单旁只放小块 **展示组件**（如 `create-problem/problem-authoring-assist-panel.tsx`），由 `index.tsx` 或页面组合。
